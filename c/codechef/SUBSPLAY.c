#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    int t, n;
    scanf("%d", &t);
    while(t--) {
        scanf("%d", &n);
        char *s = (char*) malloc(n*sizeof(char));
        scanf("%s", s);
        
    }
}